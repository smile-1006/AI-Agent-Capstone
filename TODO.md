- [x] Wire OpenWeatherMap into `mcp/tools.py` `tool_weather` to fetch tomorrow forecast (real network call)
- [x] Use env vars: `OPENWEATHER_API_KEY`, `WEATHER_API_URL` (forecast endpoint), optional `OPENWEATHER_GEOCODE_URL`
- [x] Parse API response to extract tomorrow daily forecast fields
- [x] Keep strict validation + safe failure JSON
- [x] Run test suite (`tests/`) (2 tests passed)
- [x] Wire weather evidence to executor/reviewer so frontend shows real forecast
